# OpenCode 接入

## 建议接入方式

- 以 prompt pack + workflow pack 的方式接入
- 优先复用 `skills/shared/` 与 `prompts/`

## 最小安装

1. 将本仓库作为子目录或子模块引入
2. 在 OpenCode 配置中声明角色入口
3. 通过项目级说明约束先读中文源文档

## 说明

- 当前先提供通用目录和文档适配说明
- 后续可根据 OpenCode 的具体插件规范再补 manifest

